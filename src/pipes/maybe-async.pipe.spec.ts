/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MaybeAsyncPipe } from './maybe-async.pipe';

describe('MaybeAsyncPipe', () => {
    it('', () => {
        const pipe = new MaybeAsyncPipe();
        expect(pipe).toBeTruthy();
    });
});
